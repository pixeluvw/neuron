name: neuron_cli
description: Command-line interface for Neuron. Generate projects, screens, controllers, and models with Signal/Slot architecture.
version: 1.0.0
homepage: https://github.com/pixeluvw/neuron

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  mason_logger: ^0.2.11
  path: ^1.8.3
  yaml: ^3.1.2
  recase: ^4.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  neuron:
